files:
  "/etc/nginx/conf.d/https_redirect.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      # This configuration redirects HTTP to HTTPS
      # Only applies when load balancer is configured with SSL
      
container_commands:
  01_reload_nginx:
    command: "sudo service nginx reload"
